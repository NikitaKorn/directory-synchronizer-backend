server:
  port: 8080

test:
  val: ${TEST_VAL:fromApp}

service:
  files:
    repository-path: ${REPOSITORY_PATH:src/main/resources/files/}

bot:
  name: ObsidianSynchronizer_Bot
  token: ${BOT_TOKEN:fixme}

logging:
  config: ${LOGGING_CONFIG_LOCATION:src/main/resources/logback.xml}

management:
  server:
    port: 8090
  endpoints:
    enabled-by-default: true